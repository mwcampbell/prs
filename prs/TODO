Mixer:

Modify the shout mixer output to use stdio instead of direct system
calls to open, write, and close the archive file.

Database:

Change format of log table to take a time and a recording path rather
than a recording id.

Scheduler:

Make it possible for the user to manually alter the playlist.

Make pre-scheduling configurable.

User interface:

